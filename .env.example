# Database
DATABASE_URL=postgresql://daira:daira@localhost:5432/daira

# Redis
REDIS_URL=redis://localhost:6379

# MinIO (S3-compatible storage)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=daira-media

# Redpanda (Kafka-compatible)
KAFKA_BROKERS=localhost:9092

# API
API_HOST=0.0.0.0
API_PORT=8000
API_URL=http://localhost:8000

# Web
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:8000/graphql

# Security - CHANGE THESE IN PRODUCTION
# Generate secure secrets:
# - Use at least 32 characters
# - Use a mix of uppercase, lowercase, numbers, and special characters
# - Consider using: openssl rand -base64 32
SECRET_KEY=change-this-secret-key-in-production-use-at-least-32-chars
JWT_SECRET=change-this-jwt-secret-in-production-use-at-least-32-chars

# Environment
NODE_ENV=development
